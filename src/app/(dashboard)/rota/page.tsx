export default function RotaPage() {
  return (
    <div>
      <h1 className="text-2xl font-semibold">Rota</h1>
      <p className="text-muted-foreground mt-1">Read-only view — populated by external rota system</p>
    </div>
  );
}
